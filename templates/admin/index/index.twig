{% extends 'common/layout.twig' %}

{% block body %}
    {% include '_nav'|portalpartial %}
    <div class="container">
        <div class="row">
            <div class="col-sm-12 col-md-6 col-md-offset-3">
                <h1 class="text-center">Witaj!</h1>
                <a class="list-group-item active text-center" href="{{ path_for('admin-logout') }}">
                    Wyloguj
                </a>
            </div>
        </div>
    </div>
{% endblock %}
